---
- name: Szenario 4 - Fstab Heilung (Reset)
  hosts: ziel_vm
  become: yes

  tasks:
    - name: Mounts und Daemon im Hintergrund neu laden
      ansible.builtin.shell: |
        mount -a &
        systemctl daemon-reload &
