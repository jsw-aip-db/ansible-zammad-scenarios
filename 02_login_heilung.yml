---
- name: Szenario 4 - Fstab Heilung (Reset)
  hosts: ziel_vm
  become: yes

  tasks:
    - name: Fstab Fehler bereinigen
      ansible.builtin.shell: |
        mount -a || true
        systemctl daemon-reload || true
