---
- name: Szenario 7 - KI (Ollama) Sabotage (Dienst stoppen)
  hosts: ziel_vm
  become: yes

  tasks:
    - name: Ollama Dienst stoppen und deaktivieren
      ansible.builtin.shell: |
        sudo systemctl stop ollama || true
        sudo systemctl disable ollama || true
