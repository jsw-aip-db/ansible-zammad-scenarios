---
- name: Szenario 1 - Webserver Heilung (Reset)
  hosts: ziel_vm
  become: yes

  tasks:
    - name: Nginx Service hart erzwingen und starten
      ansible.builtin.shell: |
        systemctl reset-failed
        systemctl start nginx
