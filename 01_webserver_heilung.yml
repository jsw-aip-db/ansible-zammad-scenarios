---
- name: Szenario 1 - Webserver Heilung (Reset)
  hosts: ziel_vm
  become: yes

  tasks:
    - name: Nginx direkt über die Shell starten und Autostart aktivieren
      ansible.builtin.shell: |
        systemctl unmask nginx
        systemctl start nginx
        systemctl enable nginx
