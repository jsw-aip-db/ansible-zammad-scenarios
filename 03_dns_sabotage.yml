---
- name: Szenario 3 - DNS Sabotage
  hosts: ziel_vm
  become: yes

  tasks:
    - name: Ungültigen DNS-Server erzwingen
      ansible.builtin.shell: |
        sudo bash -c 'echo "nameserver 10.99.99.99" > /etc/resolv.conf'
